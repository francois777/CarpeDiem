en:
  activerecord:
    errors:
      models:
        event:
          attributes:
            end_date:
              end_date_before_start_date: End date is before start date
              event_exceeds_max_duration: Event exceeds maximum duration
